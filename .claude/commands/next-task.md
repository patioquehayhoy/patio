Lee `docs/TASKS.md` y ejecuta la siguiente tarea pendiente (marcada con `[ ]`).

Flujo obligatorio:
1. Lee CLAUDE.md, docs/GOAL.md y docs/PATIO_DESIGN_PRINCIPLES.md antes de tocar UI
2. Ejecuta mínimo viable — sin refactor extra, sin features no pedidas
3. Valida con `npx tsc --noEmit` — debe pasar sin errores
4. Marca la tarea como `[x]` en TASKS.md
5. Escribe el reporte del ciclo en `docs/AGENT_STATUS.md` con: qué hiciste, qué encontraste, qué sigue
6. Haz commit con mensaje descriptivo en español
