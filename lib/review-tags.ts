// Etiquetas de "¿Qué destacó?" al escribir una reseña (app/resena/[id].tsx).
// Compartidas con lib/patio-stats.ts para que las reseñas sintéticas del
// panel de Reseñas (Fondero) usen exactamente el mismo vocabulario que el
// flujo real de escribir una reseña (Foodie) — no dos taxonomías distintas.
export const REVIEW_TAGS = ['Caldoso', 'Bien servido', 'Recién hecho', 'Salsa picosa', 'Trato amable', 'Tortillas hechas a mano'];
